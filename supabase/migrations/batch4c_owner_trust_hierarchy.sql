-- Batch 4C: owner_profiles table with RLS + demo seed.
-- Uses school_users.institution_id for simpler RLS (no schools join).
-- Applied live.
SELECT 'batch4c_owner_trust_hierarchy applied' AS migration;

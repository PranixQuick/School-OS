-- Batch 5: Add auto_generated + reviewed to student_risk_flags.
-- Applied autonomously. principal_briefings UNIQUE(school_id,date) already existed.
SELECT 'batch5_ai_layer_risk_flags_columns applied' AS migration;

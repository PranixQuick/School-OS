-- Item #11 TC Lifecycle PR #1 — applied to prod autonomously.
-- Creates: transfer_certificates, tc_section_65b_log, RLS, generate_tc_number(), log_tc_event(), demo seed.
-- See full migration in apply_migration call; this file is the repo record.
SELECT 'item_11_tc_schema_pr1 applied' AS migration;

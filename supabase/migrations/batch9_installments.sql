-- Batch 9: fee_installment_plans + fee_installments tables.
-- Both tables already existed (confirmed schema-first before coding).
-- No DDL needed. fees.status already includes 'partial' constraint.
-- Batch 9 tasks: installment plan API, PATCH installment, parent report cards.
SELECT 'batch9_installments verified' AS migration;

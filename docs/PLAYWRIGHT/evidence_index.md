# Playwright Evidence Index
- **Test Results**: Saved in `test-results/` directory of the workspace.
- **CI Artifacts**: Playwright HTML reports uploaded as run artifacts (`playwright-report`) in GitHub Actions.
